spring.application.name=accommodation-2306214510-be

# Database Configuration
spring.datasource.url=${DATABASE_URL_PROD:jdbc:postgresql://localhost:35002/accommodation-2306214510}
spring.datasource.username=${DATABASE_USERNAME:travelapap}
spring.datasource.password=${DATABASE_PASSWORD:2306214510}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=8080

# JWT Configuration (jika menggunakan JWT)
jwt.secret=${JWT_SECRET_KEY:defaultSecretKeyForDevelopmentOnly12345}
jwt.expiration=86400000

# CORS Configuration
CORS_ALLOWED_ORIGINS=${CORS_ALLOWED_ORIGINS:*}
